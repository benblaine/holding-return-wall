{
  "name": "holding-return-wall",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Static canvas wall of rolling holding returns for ~100 base assets.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
